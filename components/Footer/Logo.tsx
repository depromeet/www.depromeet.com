import styled from 'styled-components';
import { media } from '../../styles/theme';

const Logo = styled.img`
    ${media.mobile} {
      width: 6.4rem;
      height: 4rem;
    }
`;

export default Logo;
